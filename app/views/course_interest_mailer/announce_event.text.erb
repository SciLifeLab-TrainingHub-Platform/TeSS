Hello,

A new training opportunity related to the course "<%= @course.title %>" that you’ve shown interest in has been published.

- Event: <%= @event.title %>
<% if @event.duration.present? %>
- Duration: <%= @event.duration %>
<% end %>

<% if @event.start.present? %>
- Start date: <%= @event.start.strftime('%d %B %Y') %>
<% end %>

<% if @event.end.present? %>
- End date: <%= @event.end.strftime('%d %B %Y') %>
<% end %>

<% if @event.venue.present? %>
- Location: <%= @event.venue %>
<% end %>

You can view full details of this training event here:
<%= event_url(@event) %>

If you no longer wish to receive notifications for the course “<%= @course.title %>”, you can unsubscribe using the link below:
<%= @confirmation_cancellation_url %>

Best regards,
The Training Hub Team
