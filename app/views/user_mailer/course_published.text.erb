Hi <%= @course.user.username %>,

Your course titled "<%= @course.title %>" is now live on the Training Portal!

You can view and manage your course using the following link:
<%= course_url(@course) %>

If you've found a typo or need to update your venue you can do so via the 'Edit' button on your event page.
