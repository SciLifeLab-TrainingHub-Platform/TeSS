<div class="row materials-infobox p-3">
   Materials are self-paced training - websites, tutorials, and exercises with added description and metadata. 
   This section is ideal for supplemental or preliminary training for target audiences, for example user training for gaining access 
   to a system, or introduction to standards for registering medical devices in Sweden. 
</div>
<div class="container-fluid materials-purple">
<div class="wrapper">
  <% if TeSS::Config.solr_enabled %>
    <div id="sidebar" class="popout-sidebar">
      <%= render partial: "search/common/facet_sidebar",
                 locals: { resources: @materials_results, resource_type: Material } %>
    </div>
  <% end %>

  <div id="content">
    <h2 class="text-secondary"><%=t("features.materials.long")%></h2>

    <% content_for :buttons do %>
      <!-- Register button -->
      <%= link_to new_material_path, class: 'btn btn-secondary' do %>
        Register training material
      <% end %>
      <!-- Info -->
      <%= info_button("What are materials in #{TeSS::Config.site['title_short']}?", hide_text: true) do %>
        <%= render_markdown(materials_info) %>
      <% end %>
    <% end %>

    <%= render partial: "search/common/search_panel", locals: { resources: @materials_results,
                                                                resource_type: Material,
                                                                show_subscription_button: true } %>

    <%= render partial: 'common/masonry_grid', locals: { objects: @materials } %>

    <%= render partial: 'search/common/pagination_bar', locals: { resources: @materials } %>
  </div>
</div>
</div>